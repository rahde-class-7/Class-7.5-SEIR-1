## HANDS-ON LABS
1. Create VM with nginx installed
2. Create an unmanaged instance group from virtual machine
3. Create a load balancer with unmanaged instance group as backend
4. Cloud Armor configuration <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1.Create an policy and add rule. Then attach to load balancer <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1. Observe an default rule that denies all traffic by default <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1. first, Custom add an rule that allows all <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1.  second, Custom add an rule that allow from cloudshell only, not the local machine <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1. third, Custom add an rule that allow from local machine only, not cloudshell <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1. third, Custom add an rule that creates an two customized paths. one good, and one bad <br>
&nbsp;&nbsp;&nbsp;&nbsp; 1. fourth, Custom add an rule that creates an two customized expression that allows good traffic and blocks bad traffic <br>