creating an export
1. click `log router`
2. click `crate sink`
3. enter sink name
4. select destination service `cloud storage bucket`
5. select bucket
6. enter an query to filter
7. click create sink

naviagaed to bucket to view it
1. navigate to cloud storafge > go to the bucket
2. click on the bucket
3. deletd files created by a previous lab

note: no new log will be added to the sync. Only new logs will be added to the sync once you configure the sync

exploring other options for sinks
1. click `log router`
2. click `crate sink`
3. enter sink name
4. select destination service `cloud storage bucket`
5. select something other than bucket
6. enter an query to filter
7. click create sink
note : this option wasn't executed

option 2 for exploring other options for sinks
1. click on the query tab
1. click on resource
2. select bigquery
3. click add
click run queru
now we get logs for big query only

option 3 for exploring other options for sinks
1. click on action button
2. click create sink
3. enter sink name

    create data set <br>
    4. select destination service `BigQuery data set` <br>
    5. define data set id <br>
    6. click create data set
6. enter an query to filter
7. click create sink
