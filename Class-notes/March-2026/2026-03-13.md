class started at 1:24:00

navigated to created vm instance <br>
clicked on create vm <br>
explained pricing, regions, and vms

regions <br>
are where you put your infrastructure <br>
you have multiple options of where to place infrastructure <br>
iowa is an cheap enviornment <br>
billing changes based on where you launch your vm <br>
billing is an monthly estimate not hourly estimate
only get billed when you use the resource for atleast 1 hour. the first hour is free <br>

zones vs regions <br>
zones are data centers <br>
regions are geographical areas which hold multiple data centers <br>
data centers are a form of redundancy and backup options <br>
production enviornments use atleast 2 zones <br>

in the class you choose one zone <br>
choose n1 <br>

os and storage <br>
checked to make sure we have the correct operating system <br>
choose debian as the os everytime <br>
selected no backups <br>

networking <br>
selected allow http traffic which is port 80 <br>
do not select https <br>
http only <br>

advanced <br>
look at automation section <br>
startup script allow mush to make an website <br>
navigated to theos class repo and went to weekly lessons, clicked on week a <br>
week a contains the description of what we did <br>
went to user scripts and clicked on basic sh <br>
copied the basic.sh script and pasted it in the automation setion <br>

clicked on create <br>
now we have the vm created <br>

proof it works <br>
copy the external ip of the vm <br>
in a new tab type http:// paste ip address <br>
now we have you first website <br>

navigated to vm instance <br>
went to connect and clicked on ssh <br>
navigated to the repo <br>
navigated to weeka.txt <br>
read the instructions to `curl local host` <br>
within the vm enter the command `curl local host` <br>
within the vm enter the command `curl -s localhost | head` <br>
within the vm enter the command `systemctl status nginx --no-pager` <br>
within the vm enter the command `curl -s localhost | head` <br>
within the vm enter the command `curl -s localhost | head` <br>

stopped at around 2:00:00

### Engineering Reflection
Gemini Prompt created Reflection questions <br>
`These are the instructions for an GCP lab. Can you write me engineering reflection questions to understand each step in the configuration, why configuration decisions were made, and questions related to an production environment.`

Reflecting on these steps from an engineering perspective helps bridge the gap between "following a tutorial" and "architecting a system." 

Here are reflection questions designed to help you analyze the technical decisions made in this lab and how they translate to a professional production environment.

---

## 1. Regional & Financial Architecture
These questions focus on the trade-offs between cost, latency, and resource placement.

* **Configuration Logic:** While Iowa (us-central1) is often the cheapest region, how does geographical distance from your end-users affect the **latency** of the website?
* **Billing Analysis:** You noted that the first hour is free and billing is a monthly estimate. In a production setting where a VM runs 24/7, how would **Sustained Use Discounts (SUDs)** or **Committed Use Discounts (CUDs)** change your cost-benefit analysis compared to this lab?
* **Production Reality:** If a business requires "High Availability," why is it a risk to only deploy in one region, even if that region is the most cost-effective?

## 2. Redundancy & Zone Selection
Understanding the physical layout of the cloud is critical for disaster recovery.

* **Configuration Logic:** You chose one zone for class, but the lab mentions production environments use at least two. How does deploying across multiple zones protect an application from a **Silo/Power failure** at a single data center?
* **Resource Compatibility:** Why might certain VM machine types (like the N1 series) be available in one zone but not another within the same region?
* **Production Reality:** In a professional setup, how would a **Load Balancer** be used to distribute traffic between the two zones you mentioned?

## 3. OS, Storage, & Security
These questions look at the software stack and the attack surface of the VM.

* **Configuration Logic:** You selected Debian with "no backups." If this were a database server instead of a simple web server, what would be the technical impact of a disk failure without a **Snapshot Schedule**?
* **Security Protocol:** The lab instructions specifically say "HTTP only" (Port 80). From a security engineering standpoint, why is this inappropriate for a production site handling user data, and what is required to enable **HTTPS (Port 443)**?
* **Production Reality:** Why do many enterprises use **Custom Images** (Golden Images) instead of the default Debian public image used in this lab?

## 4. Automation & Provisioning
Reflecting on how the code actually gets onto the machine.

* **Configuration Logic:** You used a **Startup Script** to install the web server. What happens if there is a syntax error in `basic.sh`? How would you troubleshoot a script that runs during the boot process before you can SSH into the machine?
* **Scalability:** If you needed to launch 100 identical web servers, why is pasting a script into the "Automation" section manually less efficient than using **Infrastructure as Code (IaC)** tools like Terraform?
* **Production Reality:** How does the `systemctl status nginx` command help you verify that the automation script successfully reached the "Active" state?

## 5. Connectivity & Validation
Understanding how to verify work from both the "Outside-In" and "Inside-Out."

* **Outside-In:** Why does the lab have you use the **External IP** to view the website? What is the difference between an Ephemeral External IP (which might change if the VM restarts) and a **Static External IP**?
* **Inside-Out:** When you ran `curl localhost`, you tested the server from within itself. If `curl localhost` works but the external IP does not load in your browser, which GCP resource (e.g., Firewall, VPC, or Route) is likely misconfigured?
* **Production Reality:** In a secure production environment, engineers often disable External IPs entirely for VM instances. How would you "Connect via SSH" or view the web content if the VM only had a **Private IP**?

